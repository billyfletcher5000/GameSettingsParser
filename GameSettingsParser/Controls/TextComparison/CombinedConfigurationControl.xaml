<UserControl x:Class="GameSettingsParser.Controls.TextComparison.CombinedConfigurationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GameSettingsParser.Controls.TextComparison"
             xmlns:viewModels="clr-namespace:GameSettingsParser.ViewModels"
             xmlns:mvvm="http://prismlibrary.com/"
             xmlns:global="clr-namespace:GameSettingsParser"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance Type=viewModels:CombinedConfigurationControlViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <ListBox Grid.Row="0" MinHeight="120" ItemsSource="{Binding Path=Configurations}" SelectedItem="{Binding Path=SelectedConfiguration, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="{Binding Path=ConfigurationModel.DisplayName}"/>
                        <StackPanel Grid.Column="1">
                            <Label>Weight:</Label>
                            <xctk:DoubleUpDown Value="{Binding Path=Weight, Mode=TwoWay}" Increment="0.01"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ContentControl Grid.Row="1" mvvm:RegionManager.RegionName="{x:Static global:RegionNames.CombinedTextConfigurationSelected}" Margin="5"/>
        
    </Grid>
</UserControl>
