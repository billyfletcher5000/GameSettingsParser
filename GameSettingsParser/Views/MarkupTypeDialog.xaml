<Window x:Class="GameSettingsParser.Views.MarkupTypeDialog"
        x:Name="MarkupTypeDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:GameSettingsParser.Converters"
        xmlns:viewModels="clr-namespace:GameSettingsParser.ViewModels"
        xmlns:mvvm="http://prismlibrary.com/"
        MinHeight="200"
        MinWidth="300"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" 
        mvvm:ViewModelLocator.AutoWireViewModel="False"
        d:DataContext="{d:DesignInstance Type=viewModels:MarkupTypeDialogViewModel, IsDesignTimeCreatable=False}"
        mc:Ignorable="d"
        FocusManager.FocusedElement="{Binding ElementName=NameBox}">
    
    <Window.Resources>
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter" />
    </Window.Resources>

    
    <Grid Margin="10">
        <Grid.Resources>
            <!-- Default settings for controls -->
            <Style TargetType="{x:Type Label}">
                <Setter Property="Margin" Value="0,3,5,5" />
                <Setter Property="Padding" Value="0,0,0,5" />
            </Style>
            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Margin" Value="0,0,0,5" />
            </Style>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Width" Value="70" />
                <Setter Property="Height" Value="25" />
                <Setter Property="Margin" Value="5,0,0,0" />
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!-- Left,Top,Right,Bottom margins-->
        <Label Grid.Column="0" Grid.Row="0">Name:</Label>
        <TextBox Grid.Column="1" Grid.Row="0" Name="NameBox" Text="{Binding Path=TypeName, Mode=TwoWay}"/>

        <Label Grid.Column="0" Grid.Row="1">Color:</Label>
        <ComboBox Grid.Column="1" Grid.Row="1" Margin="0, 0, 0, 8"
                  ItemsSource="{Binding Path=ColorOptions}"
                  SelectedItem="{Binding Path=TypeColor, Mode=TwoWay}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Fill="{Binding Color, Converter={StaticResource ColorToBrushConverter}}" Height="10" Width="10" Margin="2, 0, 6, 0"/>
                        <TextBlock Text="{Binding Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        
        <Label Grid.Column="0" Grid.Row="2">Dynamic:</Label>
        <CheckBox Grid.Column="1" Grid.Row="2" VerticalAlignment="Center" IsChecked="{Binding Path=IsDynamic, Mode=TwoWay}" />
        
        <Label Grid.Column="0" Grid.Row="3">Relative To:</Label>
        <ComboBox Grid.Column="1" Grid.Row="3" Margin="0, 0, 0, 8"
                  ItemsSource="{Binding Path=PositionedRelativeToOptions}"
                  SelectedItem="{Binding Path=PositionedRelativeTo, Mode=TwoWay}"
                  IsEnabled="{Binding Path=HasPositionedRelativeToOptions}"/>
        <Label Grid.Column="0" Grid.Row="4">Relative Mode:</Label>
        <ComboBox Grid.Column="1" Grid.Row="4" Margin="0, 0, 0, 8"
                  ItemsSource="{Binding Path=RelativePositioningTypeValues}"
                  SelectedItem="{Binding Path=RelativePositioningType, Mode=TwoWay}"
                  IsEnabled="{Binding Path=IsPositionedRelativeToOther}"/>
        
        <Label Grid.Column="0" Grid.Row="5">Is Search Area:</Label>
        <CheckBox Grid.Column="1" Grid.Row="5" VerticalAlignment="Center" IsChecked="{Binding Path=IsSearchArea, Mode=TwoWay}" />

        <Label Grid.Column="0" Grid.Row="6" 
               ToolTip="Select which markup type should be used as a search area for this element to be found in other images, only available if this is dynamic and a second markup type is marked as 'Is Search Area'.">
            Search Area:
        </Label>
        <ComboBox Grid.Column="1" Grid.Row="6" Margin="0, 0, 0, 8"
                  ItemsSource="{Binding Path=SearchAreaOptions}"
                  SelectedItem="{Binding Path=SearchArea, Mode=TwoWay}"
                  IsEnabled="{Binding Path=HasSearchAreaOptions}"/>

        <!-- Accept or Cancel -->
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Click="OkButton_OnClick" IsDefault="True">OK</Button>
            <Button Click="CancelButton_OnClick" IsCancel="True">Cancel</Button>
        </StackPanel>
    </Grid >
</Window>

