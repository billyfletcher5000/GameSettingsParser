<UserControl x:Class="GameSettingsParser.Views.Configuration.TextComparison.BasicTextComparisonConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GameSettingsParser.Views.Configuration.TextComparison"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="10">
        <Grid.Resources>
            <!-- Default settings for controls -->
            <Style TargetType="{x:Type Label}">
                <Setter Property="Margin" Value="0,5,5,5" />
                <Setter Property="Padding" Value="0,0,0,5" />
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Margin" Value="0,0,0,5" />
            </Style>
            <Style TargetType="{x:Type ComboBox}">
                <Setter Property="Margin" Value="0, 2"/>
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
    
        <Label Grid.Row="0" Grid.Column="0">Export Mode:</Label>
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding AnalysisExportServiceTypes}" SelectedItem="{Binding SelectedAnalysisExportServiceType, Mode=TwoWay}" />
    
        <Label Grid.Row="1" Grid.Column="0">Text Comparison Mode:</Label>
        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding TextComparisonServiceTypes}" SelectedItem="{Binding SelectedTextComparisonServiceType, Mode=TwoWay}" />
    </Grid>
</UserControl>
